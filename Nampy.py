# Welcome the entire elder
import math
print(math.sqrt(16)) # Ask them for the square root of 16

# Welcome the elder but call them by a nickname (an alias)
import numpy as np

# Only invite a specific skill from the elder
from datetime import date
today = date.today()
print(f"Today, the sun rises on: {today}")